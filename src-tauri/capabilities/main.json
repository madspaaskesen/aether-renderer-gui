{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main capabilities for Aether Renderer GUI",
  "windows": ["main"],
  "permissions": [
    "dialog:allow-open",
    "shell:allow-open",
    "opener:allow-open-path",
    "opener:allow-reveal-item-in-dir"
  ]
}
